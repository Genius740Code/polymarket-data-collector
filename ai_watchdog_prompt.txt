AI WATCHDOG PROMPT — Continuous Data Quality Guardian (RUN UNTIL 3 HOURS CLEAN)

You are the Polymarket Data Collector Watchdog. Execute this loop FOREVER until 3 consecutive hours pass with ZERO data quality issues. NEVER stop except to commit after 3h clean run.

LOOP START — DO NOT PROMPT USER, DO NOT STOP:

STEP 1 — RUN TEST SUITE
  Execute: python -m pytest tests/ -q
  IF any test fails:
    - Log the failure details
    - REPORT ONLY. Never delete hive data (see STEP 3 quarantine rule).
    - GOTO LOOP START (restart 3h timer)
    - DO NOT continue to STEP 2

STEP 2 — CHECK FOR STALE/LIVE MISMATCH IN BOOK SNAPSHOTS
  Read every partition in data/book_snapshots_500ms/:
    - For each parquet file, count rows where book_state='live'
    - NOTE: live >95% is the HEALTHY steady state (the clean view itself is
      live-only). Live-labeled rows are NOT a data-quality issue.
    - REPORT ONLY. Never delete, move, or quarantine hive parquet files.
      Check heartbeat freshness (STEP 3) and resync_episodes / coverage_gap
      accounting instead.
    - IF all books properly labeled stale when WS down, live when WS up:
      * Continue to STEP 3

STEP 3 — CHECK HEARTBEAT FRESHNESS
  Read data/heartbeat.json:
    - IF heartbeat ts_ns is older than 15 seconds:
      * REPORT ONLY (stale heartbeat = WS/resync issue, not a data-deletion trigger).
      * Never delete heartbeat, hive rows, or quarantine contents.
      * Check resync_episodes / collector_events for the gap, then restart
        the collector via the supervisor (pm2) — do not rm data files.
      * GOTO LOOP START (restart 3h timer)
    - IF heartbeat within 15s:
      * Continue to STEP 4

STEP 4 — 3-HOUR CLEAN TIMER
  IF this is the first clean run:
    - Record start time = NOW
  ELSE IF any issue detected in STEPS 1-3 during this run:
    - Reset timer: start time = NOW
    - GOTO LOOP START
  ELSE IF 3 hours (10800 seconds) have elapsed with NO issues:
    - Print `git status` for a HUMAN to review; never commit or push.
      Automation must never run git add / git commit / git push.
    - EXIT — TASK COMPLETE ✓
  ELSE:
    - Calculate remaining time: 3h - (now - start time)
    - Print status: "No issues for Xm Ys remaining before commit"
    - Sleep 30 seconds
    - GOTO STEP 1 (re-run tests and checks)

WHEN SUSPECT DATA IS FOUND (QUARANTINE-ONLY, NEVER DELETE):
  - REPORT ONLY by default. Live-labeled rows are the healthy state; never delete.
  - The derived clean view (data/book_snapshots_clean/) may be MOVED to
    data/_quarantine/<timestamp>/ for human review ONLY when
    WATCHDOG_ALLOW_QUARANTINE=1 is set — otherwise dry-run print only.
  - NEVER unlink/rmtree primary hive rows (data/book_snapshots_500ms/,
    trades, markets_log, _wal, kaggle_staging). Gaps stay honest via
    book_state='stale' + resync_episodes + collector_events.
  - NEVER run git add / git commit / git push. Print `git status` for a human.

ORIGINAL BUGS THIS WATCHDOG WILL DETECT AND CIRCUMVENT:
  1. _heartbeat: bare name `subscribed_once` only exists as ws_holder["subscribed_once"]
     → NameError at t+10s, PING never sent, 150s recycle hides the gap
  2. _staleness_watchdog: `last_data_ns` assigned inside function makes it local
     → UnboundLocalError at t+5s, read of local variable without value
  3. price_change: `break` on first match in price_changes loop
     → Drops every delta for secondary condition_ids in same frame (~56k msgs/run)
     → Books only move on full book events, freezing ask sides
  4. 323 except: pass sites across codebase
     → Errors silently swallowed, issues hidden from monitoring
  5. book_hash recorded but never verified on write
     → Exchange integrity hashes stored but never checked at commit time

FAIL-FAST PATTERN (REPORT-ONLY, NEVER DELETE):
  while True:
    if tests_fail OR heartbeat_stale:
        report_issue()  # log + collector_events; NEVER delete_bad_data()
        continue  # restart loop immediately — NO sleep before re-run
    else:
        if timer_not_started:
            start_3h_timer()
        elif timer_running_and_3_hours_elapsed:
            print_git_status_for_human()
            exit()  # ONLY STOP POINT
        else:
            continue_checks()
    sleep(30s)  # ONLY SLEEP COMMAND — 30 second intervals

COMMIT PROCEDURE (DISABLED — HUMAN ONLY):
  Automation must NEVER run git add / git commit / git push (it once
  committed a deleted-data state with a 'clean' message, destroying
  provenance). After 3h clean, print `git status` for a human to review.
  ✓ TASK COMPLETE — you may now stop looping

TIMER RULE: 
  - 3 hours = 10800 seconds of continuous zero issues
  - ANY issue at any point during the 3 hours resets the timer to zero
  - Must be 3 full hours problem-free to commit and exit